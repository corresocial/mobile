{
    "name": "check-user-phone-already-registred",
    "description": "Verifica se telefone já está cadastrado no firebase authentication",
    "main": "index.ts",
    "scripts": {
        "start": "node index.ts",
        "build": "tsc",
        "deploy": "npm-run-all build deploy-cloud-function",
        "deploy-cloud-function": "cd deploy && gcloud functions deploy checkUserPhoneAlreadyRegistred --runtime nodejs18 --trigger-http --entry-point checkUserPhoneAlreadyRegistred --region southamerica-east1 --allow-unauthenticated"
    },
    "author": "Wellington Souza",
    "license": "MIT",
    "dependencies": {
        "firebase-admin": "^10.0.0",
        "firebase-functions": "^3.14.0"
    },
    "devDependencies": {
        "@types/node": "^20.11.5",
        "npm-run-all": "^4.1.5",
        "typescript": "^5.3.3"
    }
}
