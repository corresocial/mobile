{
    "name": "get-posts-by-algolia",
    "description": "Cloud Function responsável pelas buscas de posts pelo algolia",
    "main": "index.ts",
    "scripts": {
        "start": "node index.ts",
        "build": "tsc",
        "deploy": "npm-run-all build deploy-cloud-function",
        "deploy-cloud-function": "cd deploy && gcloud functions deploy searchPostsByAlgolia --runtime nodejs18 --trigger-http --entry-point searchPostsByAlgolia --region southamerica-east1 --allow-unauthenticated"
    },
    "author": "Wellington Souza",
    "license": "MIT",
    "dependencies": {
        "algoliasearch": "^4.14.3",
        "firebase-admin": "^10.0.0",
        "firebase-functions": "^3.14.0"
    },
    "devDependencies": {
        "@types/node": "^20.11.5",
        "npm-run-all": "^4.1.5",
        "typescript": "^5.3.3"
    }
}