{
    "name": "get-feed-posts",
    "description": "Cloud Function que alimenta o feed",
    "main": "index.ts",
    "scripts": {
        "start": "tsc && node deploy/index.js",
        "build": "tsc",
        "deploy": "npm-run-all build deploy-cloud-function",
        "deploy-cloud-function": "cd deploy && gcloud functions deploy getFeedPosts --gen2 --runtime nodejs20 --trigger-http --entry-point getFeedPosts --region southamerica-east1 --allow-unauthenticated"
    },
    "author": "Wellington Souza",
    "license": "MIT",
    "dependencies": {
        "firebase-admin": "^13.6.0",
        "firebase-functions": "^7.0.1"
    },
    "devDependencies": {
        "@types/node": "^20.19.27",
        "npm-run-all": "^4.1.5",
        "typescript": "^5.9.3"
    }
}