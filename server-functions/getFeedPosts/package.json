{
    "name": "get-feed-posts",
    "description": "Cloud Function que alimenta o feed",
    "main": "index.ts",
    "scripts": {
        "start": "tsc && node deploy/index.js",
        "dev": "ts-node-dev -r --respawn --transpile-only --ignore-watch node_modules index.ts",
        "build": "tsc",
        "deploy": "npm-run-all build deploy-cloud-function",
        "deploy-cloud-function": "cd deploy && gcloud functions deploy getFeedPosts --runtime nodejs18 --trigger-http --entry-point getFeedPosts --region southamerica-east1 --allow-unauthenticated"
    },
    "author": "Wellington Souza",
    "license": "MIT",
    "dependencies": {
        "firebase-admin": "^10.0.0",
        "firebase-functions": "^3.14.0"
    },
    "devDependencies": {
        "@types/node": "^20.11.5",
        "npm-run-all": "^4.1.5",
        "typescript": "^5.3.3"
    }
}