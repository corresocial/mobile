import styled from 'styled-components/native'

import { theme } from '@common/theme'

export const LoaderContainer = styled.View`
	flex: 1;
	align-items: center;
	justify-content: center;
	background-color: ${theme.colors.orange[3]};
`
