import React from 'react'
import { Text,View } from 'react-native'

function PostCategory() {
    return <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
        <Text>This is Tour Init</Text>
    </View>
}

export { PostCategory }